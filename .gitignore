# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Model files
models/
*.gguf

# Prebuilt llama.cpp/ggml shared libs for the demo image (staged at deploy
# time, uploaded to the HF Space directly; must not bloat the git repo)
lib/
*.so
*.so.*


# Local artifacts
.scope.md
.scope-turn-count
CLAUDE.md
NORTH_STAR.md
.fastembed_cache/
.ruff_cache/
.mypy_cache/
evoke_drift.log

# Personal local-only scripts (Windows build helpers, SDK config)
scripts/*.ps1
scripts/*.cmd
scripts/sdk_response.json

# Throwaway debugging repros / diagnostics from bug investigations
scripts/diag_*.py
scripts/repro_*.py
*.log

# Transient bench dispatcher stdout, not canonical result data
results/*launch.log
results/*launch_log

# Docs
docs/
archive/

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.md
paper/neurips_*
paper/*.tex
.playwright-mcp/
